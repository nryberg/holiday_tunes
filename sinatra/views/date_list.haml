%table.span-12
  %th Station
  - @results.columns.each do |col|
    %th= simple_date_time_from_number(col)
    /%th= col
  - @results.rows.each do |row|
    %tr
    %td= row
    - @results.columns.each do |col|
      %td= @results.table[[row, col]].to_i
